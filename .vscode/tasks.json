{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar e Rodar Projeto",
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "g++ src/*.cpp -I include -o main.exe && main.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}